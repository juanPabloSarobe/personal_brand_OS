services:
  core-api:
    build: ./core-api
    env_file: .env
    environment:
      - DB_PATH=/data/pbos.db
      - MEDIA_DIR=/data/media
    volumes:
      - pbos_data:/data
    ports:
      - "3000:3000"
    restart: unless-stopped

volumes:
  pbos_data:
